const paths = {
  api: { auth: `auth`, user: `user` },
  web: {
    login: `/login`,
    signup: `/signup`,
    dashboard: `/dashboard`,
    user: {
      signup: `user/signup`
    }
  }
};

export default paths;
